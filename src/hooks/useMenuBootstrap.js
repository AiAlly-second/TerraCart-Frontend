/**
 * Reserved for extracting Menu.jsx bootstrap (cart/table resolution, waitlist).
 * Logic remains in Menu.jsx — import this hook when slicing the mega-component.
 */
export function useMenuBootstrap() {
  return {};
}
