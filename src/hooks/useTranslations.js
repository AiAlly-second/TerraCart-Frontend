/**
 * Reserved for translation orchestration extracted from Menu.jsx.
 * Menu page translations currently use `postWithRetry` + local cache refs.
 */
export function useTranslationsPlaceholder() {
  return {};
}
